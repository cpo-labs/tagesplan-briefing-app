# better-auth 1.x has a peer-dep conflict with React 19. The override is
# safe because better-auth does not actually use React APIs at runtime.
# Remove this once better-auth ships React 19 in peerDependencies.
legacy-peer-deps=true
